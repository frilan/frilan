{
  "name": "frilan",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "npm run build -w @frilan/models"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.20.0"
  }
}
